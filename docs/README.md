# Documentation index

Placeholder. Artifacts will land here as each roadmap phase completes.

| Path | Phase | Contents |
|------|-------|----------|
| `docs/openapi.json` | P0 | Auto-generated FastAPI OpenAPI spec |
| `docs/schemas/` | P1 | JSON schemas for VLM extraction outputs (Pydantic export) |
| `docs/diagrams/` | P2+ | Architecture, LangGraph flows, KG schema, RBAC matrix |
| `docs/adr/` | Ongoing | Architecture decision records |

Keep diagrams as source (Mermaid `.mmd` or Excalidraw `.excalidraw`) plus a rendered PNG, so they stay reviewable in PRs.
